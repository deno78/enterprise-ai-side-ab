# exam-alpha / exam-beta ソースコード評価レポート

---

## exam-alpha 評価

### 構造・設計: ★★☆☆☆ (2/5)
- **単一ファイルアーキテクチャ**: Flaskルート、モデル、翻訳辞書全てが `app.py` (607行) に集約。保守性に課題
- **Blueprint不使用**: ルーティングのモジュール分割なし
- **翻訳データの重複**: Python側 (app.py) とJS側 (index.html) に同じ翻訳辞書があり、更新時に不整合が起きやすい
- モデルは `models.py` に1ファイルでまとまっているが、合理的な範囲

### Python品質: ★★★☆☆ (3/5)
- ルート定義は素直で可読性は高い
- エラーハンドリングは最小限（`get_or_404` / 簡易バリデーション）
- `PIL.Image` の import fallback (`pil` モジュール) は動作保証が曖昧
- `seed_data()` が起動時に毎回呼ばれる設計
- パスワードは bcrypt で適切にハッシュ化

### JS/HTML品質: ★★☆☆☆ (2/5)
- **Vue 3 SFC不使用**: 単一HTMLに全てのテンプレート・ロジックを埋め込み（690行の巨大ファイル）
- デリミタを `{{ }}` から `{[ ]}` に変更する独自設定
- コンポーネント分割なし、全て `v-if` でページ切り替え
- テンプレートとロジックが渾然一体で可読性低
- 翻訳辞書をHTML内にハードコード

### CSS品質: ★★★★☆ (4/5)
- カスタムプロパティ活用、レスポンシブ対応、Utilityクラス整備。シンプルで良質

### 機能範囲: ★★★☆☆ (3/5)
- 施設検索・予約・決済・PDF・QRコード生成・多言語対応 — 基本機能は一通り実装
- 管理画面なし、権限分掌なし
- 営業時間が9-17時固定

---

## exam-beta 評価

### 構造・設計: ★★★★★ (5/5)
- **Blueprint分割**: auth / facilities / reservations / payments の4つに明確分離
- **モデルの分離**: 各モデルが独立したファイル（User, Facility, Reservation, Payment, ReservationLog）
- **サービス層**: `pdf_service.py` としてPDF生成を分離
- **Configクラス**: 設定を一元管理
- **デザインパターン**: app factory パターン（`create_app()`）
- AGENTS.md によるエージェント設計思想、アーキテクチャ文書あり

### Python品質: ★★★★★ (5/5)
- 構造化エラー応答（RFC 7807 Problem Details風）
- Flask-Login によるセッション管理
- バリデーションが充実（各フィールド個別チェック、エラー収集後一括返却）
- ロールベース制御（citizen / staff）
- ページネーション・フィルタリング（price, capacity, type, q）が充実
- ReservationLog による操作ログ記録
- 営業時間 9-21時と現実的、時間帯重複チェックも適切

### JS/HTML品質: ★★★★★ (5/5)
- **コンポーネント分割**: 13のJSコンポーネントファイルに分割（関心の分離）
- **vue-i18n**: 国際化をプラグインで適切に管理
- **Bootstrap 5**: レスポンシブUIを効率的に構築
- **SPAルーティング**: `navigate()` によるクライアントサイドルーティング
- テンプレートとロジックがコンポーネント単位で明確に分離
- Storeパターン（`store.js`）で状態管理

### CSS品質: ★★★☆☆ (3/5)
- Bootstrapベースで補助的なスタイルのみ。カスタムCSSは33行と最小限
- Bootstrap依存が強く、独自デザイン性は低い

### 機能範囲: ★★★★★ (5/5)
- 全機能（施設CRUD、予約CRUD、空き状況確認、決済、PDF、QR、多言語、管理画面）
- **管理画面**: 施設管理・予約一覧（staffロール）
- **操作ログ**: 予約作成・変更・キャンセルをすべて記録
- CI/エージェント設定まで含めたプロジェクト管理

---

## 総合比較

| 観点 | exam-alpha | exam-beta |
|---|---|---|
| **アーキテクチャ** | モノリシック (1ファイル) | モジュール分割 (Blueprint) |
| **Python品質** | 可読性良好だが粗い | 堅牢・構造化・バリデーション充実 |
| **JS品質** | 単一HTML埋め込み | 13コンポーネントに分割、SPA |
| **CSS品質** | 独自CSS (充実) | Bootstrap依存 (薄い) |
| **多言語対応** | 辞書重複あり | vue-i18n で適切管理 |
| **エラーハンドリング** | 最小限 | Problem Details形式で充実 |
| **管理機能** | なし | 施設・予約管理画面あり |
| **ログ監査** | なし | ReservationLog あり |
| **認可制御** | なし（認証のみ） | ロールベース (citizen/staff) |
| **ドキュメント** | spec.mdのみ | アーキテクチャ文書 + エージェント設計書 |
| **拡張性** | 低い | 高い（プラグイン・Blueprints・サービス層） |

**総評**: exam-beta はアーキテクチャ・コード品質・機能・拡張性の全てで exam-alpha を大幅に上回る。exam-alpha は簡易プロトタイプとして成立しているが、プロダクション品質には程遠い。exam-beta は実運用を意識した堅牢な設計がなされている。
